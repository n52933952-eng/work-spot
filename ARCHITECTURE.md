# Work Spot - System Architecture

## 📱 Mobile App (React Native) - للموظفين فقط

### الأدوار المستخدمة:
- `employee` - الموظف العادي

### الوظائف المتوفرة:
1. **المصادقة**
   - تسجيل الدخول (Email/Employee Number + Password)
   - إنشاء حساب جديد (للموظفين الجدد)
   - تسجيل الدخول بـ Face ID/Fingerprint
   - تفعيل/إلغاء Face ID
   - تغيير كلمة المرور

2. **الحضور**
   - تسجيل الحضور (Check-in) مع GPS + Face ID
   - تسجيل الانصراف (Check-out) مع GPS + Face ID
   - عرض حالة اليوم (حاضر/متأخر/غائب/عطلة)
   - عرض سجل الحضور الشهري
   - استخدام QR Code عند ضعف الشبكة

3. **الإجازات**
   - إنشاء طلب إجازة
   - عرض طلبات الإجازات (الحالة، الموافقة، الرفض)
   - إلغاء طلب إجازة (قبل المراجعة)

4. **الإعلانات**
   - عرض الإعلانات الموجهة للموظف
   - قراءة الإعلانات وتتبع القراءة

5. **الملف الشخصي**
   - عرض معلومات الموظف
   - عرض نقاط الحضور
   - تفعيل Face ID
   - تسجيل الخروج

### APIs المستخدمة:
- `GET /api/auth/me` - معلومات المستخدم
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/register` - إنشاء حساب
- `POST /api/attendance/checkin` - تسجيل الحضور
- `POST /api/attendance/checkout` - تسجيل الانصراف
- `GET /api/attendance/today` - حالة اليوم
- `GET /api/attendance/monthly` - الحضور الشهري
- `POST /api/leaves` - إنشاء طلب إجازة
- `GET /api/leaves/my` - طلبات الإجازات الخاصة بي
- `GET /api/announcements/my` - الإعلانات الموجهة لي
- `GET /api/qrcode/my` - QR Codes الخاصة بي

---

## 💻 Web App (React JS) - للإدارة فقط

### الأدوار المستخدمة:
- `admin` - المدير العام
- `hr` - الموارد البشرية
- `manager` - المدير

### الوظائف المتوفرة:

1. **لوحة التحكم (Dashboard)**
   - عدد الموظفين الحاضرين الآن
   - الموظفون المتأخرون
   - الموظفون الغائبون
   - المواقع الجغرافية للحضور (خريطة)
   - طلبات الإجازات المعلقة
   - إحصائيات الشهر

2. **إدارة الموظفين**
   - عرض جميع الموظفين
   - إضافة موظف جديد
   - تعديل معلومات الموظف
   - إيقاف/تفعيل الموظف
   - تعيين الموقع الجغرافي للموظف
   - عرض نقاط الحضور

3. **إدارة المواقع الجغرافية**
   - إضافة موقع جديد (رئيسي، فرع، مؤقت، ميداني)
   - تعديل الموقع (الإحداثيات، نصف القطر)
   - حذف الموقع
   - عرض جميع المواقع
   - إنشاء الموقع الافتراضي

4. **إدارة العطل الرسمية**
   - إضافة عطلة رسمية
   - تعديل العطلة
   - حذف العطلة
   - عرض التقويم السنوي
   - استيراد العطل من CSV

5. **إدارة الإجازات**
   - عرض جميع طلبات الإجازات
   - مراجعة الطلبات (موافقة/رفض)
   - عرض الإجازات بالموظف
   - عرض إحصائيات الإجازات

6. **التقارير**
   - تقرير الحضور الشهري (PDF)
   - تقرير التأخيرات (PDF)
   - تقرير العمل الإضافي (PDF)
   - تصدير التقارير

7. **الإعلانات**
   - إنشاء إعلان جديد
   - توجيه الإعلانات (جميع الموظفين، قسم، دور، موظفين محددين)
   - تعديل الإعلان
   - حذف الإعلان
   - عرض إحصائيات القراءة

8. **Live Attendance Board**
   - عرض لوحة الحضور المباشرة
   - الموظفون الحاضرون الآن
   - الموظفون الذين سجلوا الانصراف
   - تحديث فوري

### APIs المستخدمة:
- `GET /api/dashboard` - لوحة التحكم (Admin/HR/Manager only)
- `GET /api/dashboard/live-board` - لوحة الحضور المباشرة (Public for display)
- `GET /api/dashboard/employees` - جميع الموظفين (Admin/HR/Manager only)
- `GET /api/locations` - جميع المواقع (Admin only)
- `POST /api/locations` - إنشاء موقع (Admin only)
- `POST /api/locations/default` - الموقع الافتراضي (Admin only)
- `GET /api/holidays` - جميع العطل (Admin only)
- `POST /api/holidays` - إنشاء عطلة (Admin only)
- `POST /api/holidays/import` - استيراد العطل (Admin only)
- `GET /api/leaves/all` - جميع الإجازات (Admin/HR only)
- `PUT /api/leaves/:id/review` - مراجعة الإجازة (Admin/HR only)
- `GET /api/reports/monthly` - تقرير شهري (Admin/HR/Manager only)
- `GET /api/reports/late` - تقرير التأخيرات (Admin/HR/Manager only)
- `GET /api/reports/overtime` - تقرير العمل الإضافي (Admin/HR/Manager only)
- `POST /api/announcements` - إنشاء إعلان (Admin/HR/Manager only)
- `GET /api/announcements/all` - جميع الإعلانات (Admin/HR/Manager only)

---

## 🔐 Role-Based Access Control

### الموظف (Employee)
- ✅ تسجيل الحضور/الانصراف
- ✅ عرض بياناته الشخصية
- ✅ طلب إجازة
- ✅ عرض الإعلانات الموجهة له
- ❌ لا يمكنه عرض بيانات موظفين آخرين
- ❌ لا يمكنه إدارة المواقع
- ❌ لا يمكنه إدارة العطل
- ❌ لا يمكنه مراجعة الإجازات

### الموارد البشرية (HR)
- ✅ كل ما يمكن للموظف فعله
- ✅ عرض جميع الموظفين
- ✅ مراجعة طلبات الإجازات (موافقة/رفض)
- ✅ إنشاء إعلانات
- ✅ عرض التقارير
- ✅ عرض لوحة التحكم

### المدير (Manager)
- ✅ كل ما يمكن لـ HR فعله
- ✅ لوحة التحكم الشاملة
- ✅ عرض التقارير الشاملة

### المدير العام (Admin)
- ✅ كل الصلاحيات
- ✅ إدارة المواقع الجغرافية
- ✅ إدارة العطل الرسمية
- ✅ إدارة الموظفين كاملة
- ✅ إعدادات النظام

---

## 📝 ملاحظات مهمة

1. **Mobile App للموظفين فقط**: لا يمكن للموظفين الوصول لـ Admin Panel
2. **Web App للإدارة فقط**: لا يحتوي على تسجيل حضور للموظفين (فقط Mobile)
3. **نفس Backend**: كلا التطبيقين يستخدمان نفس الـ APIs
4. **الأمان**: التحقق من الأدوار يتم في الـ Backend لكل طلب
5. **Mobile App يستخدم IP**: `http://192.168.100.66:5000/api`
6. **Web App يستخدم localhost**: `http://localhost:5000/api` (أو نفس IP)

---

## 🎯 الخطوات التالية

### Mobile App (React Native) - ✅ جاهز
- ✅ Authentication
- ✅ Check-in/Check-out
- ✅ Profile
- ⏳ Leaves Management
- ⏳ Monthly Attendance View

### Web App (React JS) - ⏳ قيد التطوير
- ⏳ Login/Register for Admin
- ⏳ Dashboard
- ⏳ Employee Management
- ⏳ Location Management
- ⏳ Holiday Management
- ⏳ Leave Review
- ⏳ Reports (PDF generation)
- ⏳ Announcements
- ⏳ Live Attendance Board












